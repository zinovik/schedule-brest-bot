version: '3'

services:
  database_redis:
    image: redis:latest
    restart: always
    ports:
      - 9379:6379

  api:
    build: .
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules/
    ports:
      - 9000:9000
    depends_on:
      - database
