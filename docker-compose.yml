version: '3'

services:
  database:
    image: redis:latest
    restart: always
    ports:
      - 9379:6379

  api:
    build: .
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules/
    ports:
      - 6000:6000
    depends_on:
      - database
