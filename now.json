{
  "version": 2,
  "env": {
    "IS_NOW": "true"
  },
  "builds": [
    {
      "src": "./src/**/*.ts",
      "use": "@now/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/src/$1" }
  ]
}